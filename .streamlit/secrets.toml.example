# Exemplo de configuração de secrets
# Copie este arquivo para .streamlit/secrets.toml e preencha

[google_earth_engine]
# Opção 1: Cole o JSON da service account em base64
service_account_b64 = "SEU_JSON_EM_BASE64_AQUI"

# Opção 2: Ou use o arquivo local service_account.json na raiz

# Para converter JSON para base64 (Linux/Mac):
# base64 -i service_account.json | tr -d '\n'
